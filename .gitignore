# Node
node_modules/
build/
dist/
.env
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
*.whl

# OS
.DS_Store

# Project-specific generated files
AGENTS.md

# Local dev fixtures for manual GUI testing (regenerate: scripts/make_dev_fixtures.py)
python/tests/data/

# Built frontend copied into the package at conda-build time (conda-recipe/build.sh);
# the `build/` rule above already covers its build/ subdir, this covers the HTML/icon.
python/pxviewer/frontend/

# geostd monomer library (large; git clone https://github.com/phenix-project/geostd)
/geostd/

# Richardson-lab validation reference data (large; scripts/setup_reference_data.sh)
/reference_data/

# Local debugging artifacts (perf screenshots, capture logs)
debug/
